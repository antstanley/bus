{"affected_locations": ["packages/store-fs/src/index.ts:432-436", "packages/store-fs/src/index.ts:212-224"], "artifact": "artifacts/02_discovery/raw_candidates.jsonl (cand-03)", "candidate_id": "cand-03", "parent_reconciliation": "independent reviewer SK candidate 'a' \u2014 closed as not plausible; reconciled by the parent with primary-source verification of packages/store-fs/README.md", "receipt": "discovery", "recorded_at": "2026-09-10T10:50:00Z", "revision": "fe382d305db19078446750020ede9f6b9a0d5348", "scan_scope": "f37b83b..fe382d305db19078446750020ede9f6b9a0d5348", "source": "OS watcher event filenames (untrusted, possibly undefined)", "status": "candidate_open"}
{"artifact_paths": [], "candidate_id": "cand-03", "confidence": "medium", "confidence_rationale": "Static trace plus a real local watcher observation; the CI platform that produced the original undefined filename was not reproduced.", "counterevidence_or_proof_gap": "A wake/read/wake loop would need a platform that reports .git-internal writes with an undefined filename AND a consumer whose own reaction writes into .git with the same undefined reporting. Repository evidence does not show that combination; the change strictly removes a crash for the same input. The null-filename bypass already existed at base and is a documented accepted limitation; the delta merely extends the same handling to the input class that previously threw an uncaught TypeError inside the fs.watch callback. No in-tree consumer syncs unboundedly on a hint.", "disposition": "suppressed", "evidence": "Probe (bun 1.4.0, macOS, recursive watch on /tmp): .git writes reported concrete filenames ('rename .git/HEAD', 'rename .git/index'), so the existing .git filter still applies on this platform; no undefined filename observed. The watcher callback returns early when hintConsumers.size===0 and coalesces with hintDebounceMs (default 100ms). The pre-change behaviour for an undefined filename was a TypeError thrown inside the fs.watch callback. Independent clean-context reviewer re-ran bounded probes on bun 1.4.0 on Linux (cached local binary, no network) and macOS: git bookkeeping under .git/** produced 350 watcher events, 100% string filenames still suppressed by isGitMetadata; the only reproducible filename-less event was removal/replacement of the watched root itself, and that event is terminal (the old watcher is bound to the deleted inode). Independent parent verification: packages/store-fs/README.md:18-29 (byte-identical at base f37b83b) already documents this exact class as an accepted performance limitation bounded by the 100 ms debounce and the mandatory authoritative read after every hint.", "method": "static source trace + bounded local fs.watch observation probe (outside the repository)", "parent_reconciliation": "independent reviewer SK candidate 'a' \u2014 closed as not plausible; reconciled by the parent with primary-source verification of packages/store-fs/README.md", "receipt": "validation", "remaining_uncertainty": "Bun on Linux CI previously delivered an undefined filename in this project; if that recurs specifically for .git-internal writes the loop hypothesis should be re-checked then.", "rubric": ["attacker-controlled source identified", "closest control identified", "bypass plausible", "impact reachable", "counterevidence considered"]}
