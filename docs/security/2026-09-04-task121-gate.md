# Security Re-Gate — task 121 fix verification (hooks CR normalization + pi default author)

Focused clean re-gate: verify the 2 LOW fixes from the 2026-09-03 gate
(20260903T-121-gate) and confirm no regressions. Review-only, static reading,
no execution, no network. Bundle-local writes only.

## 1. VERDICT

**ACCEPT** — both fixes verified on the pinned tree; no regressions found; no new findings on changed lines.

## 2. PASS/FAIL per verification point

1. **F1 fixed — PASS.** board-hook.ts:541 normalizes `/\r\n|[\r\u000b\u000c\u001c-\u001e\u0085\u2028\u2029]/g` → `\n` — the full set CR, VT, FF, FS/GS/RS, NEL, LS, PS; alternation order makes CRLF a single break; every mapping shrinks (CRLF 2→1, NEL 2→1, LS/PS 3→1) or is 1:1 (CR/VT/FF/FS/GS/RS), so cap math is unaffected; `quoteUntrusted`:536-538 and `renderQuotedSection`:524-526 normalize before `.split("\n")`. Extended fixtures exist (hooks.test.ts:19-30, used :83-105 and :165-190) and assert the right invariants: no raw separator survives (hooks.test.ts:97, :181), every lookalike closer renders prefixed `| ` and exactly ONE column-0 closer of each kind remains (hooks.test.ts:98-104, :182-189), CRLF renders as a single break via the exact `| before-CRLF\n| [/UNTRUSTED CONTENT]` assertion (:99, :183), and closers + final newline survive the truncated path (`toEndWith("[/UNTRUSTED CONTENT]\n</board-messages>\n")`, hooks.test.ts:189).
2. **F2 fixed — PASS.** install.ts:165 digest is `.slice(0, 16)` (16-hex); prefix capped at `.slice(0, 12)` (:166) so the digest path yields ≤ 12+1+16 = 29 ASCII chars and `pi-` + 29 = **≤32 bytes by construction** (`normalized` contains only `[a-z0-9_-]` after :153-157); `normalized.length > 29` trigger unchanged. Both READMEs qualified: cli/README.md:99-102 ("case-only variants intentionally share one identity… digest reduces collision risk rather than guaranteeing that all distinct source hostnames stay distinct… remains within the 32-byte agent-name limit") and hooks/README.md:49-52 ("distinct identities with high probability rather than a collision-free guarantee. ASCII-case-only hostname variants intentionally share one identity"). Explicit `--as` still wins (install.ts:48-50; install.test.ts:382-389); invalid hosts still fail closed (install.ts:158-160; install.test.ts:368-369). ASCII-case shared identity is asserted as intentional (install.test.ts:348).
3. **No regressions — PASS.** Spot-recheck on the pinned current files: `|`-prefix on every content line (board-hook.ts:525, 537); cap keeps header/notice/inner-close outside the fitted body with fail-closed re-check (:478-482 `fixed > budget → ""`; final re-check :466 `output > cap → consumed 0`); UTF-8 continuation-safe slicing (:512 backs up over continuation bytes); whoPage bounded at MAX_WHO_LIMIT with content-independent truncation notice (cli/index.ts:109-115, :450, :452-454 — file byte-identical to the sealed prior gate pin, sha f647e944…); no new spawn surface — the fix delta touches only digest math and the separator regex, `pi.exec`/MCP argv arrays intact (install.ts:192, 261, 264-267, 325-329); owned-merge idempotent install unchanged (install.ts:86-94, :223-225; install.test.ts:313-338); identity propagation intact — one resolved `author` threaded into `hookConfig`/`cliConfig`/mcp env (install.ts:243-254; install.test.ts:371-389).
4. **Scope discipline — PASS.** Fix delta (prior gate snapshot → current pinned files) touches exactly board-hook.ts (1 line), hooks.test.ts, install.ts (comment + 2 lines), install.test.ts, cli/README.md, hooks/README.md; cli/index.ts is byte-identical to the prior gate pin (allowed "if at all"). Nothing extra. Observation (not a violation): untracked `packages/index/src/tasks.ts` sits in the working tree outside the 7-file scope — other-workstream activity, same category the prior gate explicitly ignored; it cannot enter the scoped diff.

Standard pass on changed lines — PASS: no error-message interpolation in the delta (only messages are pre-existing JSON.stringify'd ones), no jq/shell logic added, no secrets (digest input is the ASCII-case-folded hostname, output is hex; hostname itself never echoed).

## 3. Findings (ranked)

None. Both prior LOW findings are fixed (F1: board-hook.ts:541 now covers the full separator set requested by the gate; F2: 16-hex digest with 12-char prefix at install.ts:165-166, and both README claims weakened to match the mechanism). No new defects on the changed lines.

## 4. Suppressed / lower-severity notes

- INFO (unchanged, previously disclosed): any host able to set its hostname can clone a victim's derived identity exactly regardless of digest length; mitigated by the collision warning and explicit `--as`.
- INFO: hooks.test.ts:189 `toEndWith` proves the outer closers survive truncation; inner-close newline at arbitrary cap edges remains covered by the adjacent-cap tests (:144-163) as in the prior gate.
- INFO: header fields author/board/id remain validated by core (per prior gate, core/src/post.ts:265-269), so no line breaks can reach the framing header; unchanged by this delta.

## 5. Pin & drift

- Baseline: f5f891c (full: f5f891c0f5ba2f7451477a5fdf411251f4bd104a). HEAD at pin and at seal: identical to baseline.
- Pinned files: 7 (artifacts/03_snapshot/ + SHA256SUMS); re-verified at seal: all 7 OK.
- Drift: NONE — fresh scoped diff (`git diff f5f891c -- <7 paths>`) byte-identical (`cmp`) to pinned artifacts/change.diff.
- Fix delta isolation: diff of prior-gate change.diff vs this change.diff contains only the F1 hunks (board-hook.ts:541, hooks.test.ts fixture array + rewritten separator test + new truncated-path test) and the F2 hunks (install.ts digest/prefix, install.test.ts 16-hex + case-equality + exact-32 assertions, both README qualifications).

## 6. Sealed bundle

/Volumes/Delorean/code/security-scans/sidekick-tmp/20260904T-121-fix-regate/
- artifacts/change.diff (pinned fix-inclusive scoped diff)
- artifacts/fresh-verify.diff (drift check copy)
- artifacts/03_snapshot/ (7 pinned files + SHA256SUMS)
- artifacts/report.md (this report)
- SEALING.txt (final manifest hashes)

## 7. Could not check

- Nothing in the 7-file scope was left unread; all changed lines, both test files, and both README sections were reviewed from pinned copies.
- Not checkable by construction: runtime behavior (tests not executed per hard rules; no bun/tsc/shellcheck per instructions). All verdicts are from static reading of the pinned copies plus the sealed prior gate.
