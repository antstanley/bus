# Task 404 — Final Clean Security Re-Gate (review-only)

Scope authority: frozen nine-path diff vs baseline 35df4b9, SHA256
`106434fa950c31c067f499aa5b4f20b0123ed10807339c96711f61ba942fef25`.
Review performed on pinned snapshot only; repo untouched; no network; no build/test.

## VERDICT: ACCEPT

No new findings. The disclosed wrong-file restore left no residue: the final
`packages/store-fs/src/index.ts` is baseline-complete plus intended additive
changes only (its diff removes nothing except the import line and constructor
signature, both superseded by design). One pin transcription discrepancy noted
below; per dispatch, the frozen diff hash is the scope authority and it matches.

## Pin

- HEAD at pin: `35df4b977810ef8ba344b5015bfbe3f521d9c19e` (equals the baseline
  commit; all shipped 404 work is working-tree changes vs that commit).
- Frozen diff hash match: YES (regenerated nine-path diff hashes exactly
  `106434fa…fef25`, at pin and again at seal).
- `packages/store-fs/src/index.ts` pin check: lead's pin
  `…6604a906…` does NOT verify. Actual:
  `ae9edd2d487274f53b288e5f6604f906aee2ea32a0cd23ab004dae4dbc9f8e30` — a single
  hex-digit transcription error (position 32: `a` vs `f`). File content is
  bound by the frozen diff hash, which matches; treat the lead's pin as a typo.
- Per-file SHA256:
  - `62f120d8764a6a0fd692c7d39670fa0272d482af4509b2702cd03e2a83af5c0d` packages/core/src/board.ts
  - `18286adec5f97a19342c209ed6b28b84220db7bfbfb070afd300ea3ac4d73cc2` packages/core/src/store.ts
  - `ccf44fb9089314bbe3790f0e920674df0f95d72852420b80646b0ae2d05362b1` packages/core/test/board.test.ts
  - `734b92d8f458733b5e3e19bb321f0132bdfcde8e5b8efef40bae94809ce74416` packages/store-fs/README.md
  - `ae9edd2d487274f53b288e5f6604f906aee2ea32a0cd23ab004dae4dbc9f8e30` packages/store-fs/src/index.ts
  - `793bc3307d152787432099356a36482b2a94ebc8282b55d6a01514bf2419d988` packages/store-fs/test/store-fs.test.ts
  - `4fa282afc4aa7088fd52b5e51809ae976f2f3b19bf49a4519f26b2e7158aca0e` packages/store-git/README.md
  - `76df64c4e06016967519df262f5976ba7515104367cd0e51ed8464a7961031f7` packages/store-git/src/index.ts
  - `de1646ecc17780c985b7c89089ac6903a75f5403851f110435889d7d84483b2f` packages/store-git/test/store-git.test.ts
- Drift at seal: NO (fresh nine-path diff byte-identical to pinned change.diff;
  all nine snapshot hashes re-verified OK).

## Point-by-point

1. Three new fixes — all PASS.
   - (a) Symlink-aware core.hooksPath containment: PASS —
     `packages/store-git/src/index.ts:333-351` resolves git dir, worktree, and
     hooks dir through the filesystem (`resolveExisting`, :497-511) and gates on
     `isInside` over REAL paths (:487-490), skipping external/unresolvable
     paths fail-closed; `resolveExisting` returns undefined on any non-ENOENT
     resolution failure, and a dangling tail fails at mkdir (caught, :352-359).
     Whole-file view confirms hooks are never touched before the realpath gate
     and hook bodies/paths derive only from post-gate values. Regression tests:
     `packages/store-git/test/store-git.test.ts` "skips hook installation when
     core.hooksPath resolves outside through a symlinked ancestor" and
     "installs functional wake hooks through an in-repo core.hooksPath symlink"
     (inside-repo symlinks still work).
   - (b) Iterator cancellation/lifecycle: PASS —
     `packages/store-fs/src/index.ts:163-188` registers the consumer inside the
     generator body (an unstarted iterator holds nothing), overrides
     `iterator.return` to close the consumer first so a pending `next()`
     resolves immediately instead of waiting for the next filesystem event;
     `ensureWatcher`/`failWatcher`/`stopWatcher` (:190-244) guard every path on
     watcher identity, clear `hintTimer` on every stop/fail path, and release
     the watcher at the last consumer; `HintConsumer` (:358-407) removes its
     abort listener on close and fail-closes subsequent `next()` after failure.
     Whole-file view confirms no other code touches `hintTimer`/`watcher`, and
     the intentional close path cannot re-enter `failWatcher` (identity guard).
     Board side: `packages/core/src/board.ts:270-286` hint pump, :340-345
     finally (abort → close → unforward → await pump), `WakeSignal` :414-452
     clears its timer on every finish path, `forwardAbort` :454-460;
     `GitStore.hint` (`packages/store-git/src/index.ts:165-169`) delegates
     `yield*`. Tests: `packages/store-fs/test/store-fs.test.ts:177-193` (prompt
     return while next() pending), :195-235 (one closing consumer leaves
     watcher and others alive), :161-175 (error/close end iterators);
     `packages/core/test/board.test.ts` `hintCleanups === 1` plus
     missed/spurious/failed/closed fallback-poll tests.
   - (c) Default-max compatibility: PASS — `packages/core/src/board.ts:239`
     defaults the backoff cap to `max(30_000, intervalMs)` so an omitted
     `maxIntervalMs` never sits below a caller's large `intervalMs` (previously
     possible breakage), validation :241-243 still rejects explicit
     `maxIntervalMs < intervalMs`; backoff :335-338 caps at `maxInterval`.
     Docs updated in `WatchOptions` (:32-49). Tests: board.test.ts "watch
     allows a large intervalMs when maxIntervalMs is omitted" (intervalMs
     60_000 polls once; explicit maxIntervalMs below interval rejects) and
     "rejects invalid watch options". Note: `list()` Infinity-tolerance
     (`packages/store-fs/src/index.ts:121-124`) and its test
     (`packages/store-fs/test/store-fs.test.ts:108-112`) are byte-identical to
     baseline — list default-max behavior is baseline-stable, unchanged here.
2. Entire-file review of `packages/store-fs/src/index.ts` (501 lines, every
   line): PASS — no injection sinks (no shell, no eval; filenames from watcher
   events are classified only, :203-215, :419-423, never resolved or opened);
   no path traversal (key segments validated :409-417, `safeParent` re-verifies
   on disk with lstat symlink rejection and realpath containment :256-300, get
   uses O_NOFOLLOW :107); no unbounded growth (consumer set is closed on every
   exit path, hint state is a boolean, list reads limit+1); error handling
   fail-closed (watcher error/close end iterators with rejection; list skips
   only ENOENT/EACCES/EPERM subtrees :330-339); no stale-code remnants
   (naming consistent, no duplicated/orphaned fragments, no dead branches; diff
   vs baseline shows zero unintended deletions — the restore reconstruction is
   faithful); no secrets.
3. Prior 404 gate PASS points — all hold: external hooksPath preservation
   (store-git :348-351 + test with byte-identical sentinel, mode preserved,
   missing external path never created); atomic owned-hook replacement
   (`refreshOwnedHook` :412-429 wx-temp + fsync + rename, foreign hooks and
   symlink hooks skipped :384-394, EEXIST race yields to the other installer
   :402-408; tests cover drift refresh, idempotence, no temp leftovers, foreign
   content/mode untouched); unknown-event fail-closed + accurate docs
   (`changes` :143-163 TOKEN regex plus cat-file type check falling back to
   `allTrackedKeys` :312-316; both README additions match implementation,
   including the honest null-filename performance caveat in
   `packages/store-fs/README.md`); bounded watch surface (one shared recursive
   root watcher per FsStore, never per-event, released at last consumer);
   contentless hints (payload-free notify :376-385; contract documented
   `packages/core/src/store.ts:52-58`); argv-array git (`Bun.spawn(["git",
   "-C", …])` :447-470 with GIT_* env scrubbed and prompt disabled); no
   delivery targets from presence (hints never carry keys; Board.watch
   delivers only via since()/changes()/reconcile content, board.ts:305-338;
   wake hook writes only an ignored `.board-wake`, never under `.git`,
   :360-377, regression-tested).
4. Scope discipline: PASS — change.diff contains exactly the nine frozen paths
   and no foreign content. (The repo working tree carries other unrelated dirty
   files; they are outside the frozen scope and were not reviewed.)
5. Root validation: NOT RUN (read-only gate per hard rules). A fresh full
   `bun install`/`bun test` root validation remains pending and is the lead's
   arranged deliverable; recommended before merge.

## Findings ranked

None.

## Suppressed (7)

1. INFO — packages/store-fs/src/index.ts:65 and packages/store-git/src/index.ts:415 use the runtime-global `crypto.randomUUID()` without an import; the pattern pre-exists at baseline (store-fs line 41), the repo targets Bun (`Bun.spawn`, `bun:test`), and it is not a restore artifact.
2. INFO — packages/core/src/board.ts:296 counts a hint as `hinted=false` when the poll deadline has already passed; the hint stays queued and wakes the next wait, so correctness and cadence are unaffected.
3. INFO — packages/store-fs/src/index.ts:114 `file.close()` in `finally` can mask the caught read error if close itself fails; standard pattern, unchanged from baseline.
4. LOW (accepted threat model, carried from prior gate) — lstat/realpath verification in `safeParent`/`ensureWakeHooks` is not atomic with the subsequent write; full mitigation would need *at file-descriptor operations; realpath containment and rename-not-follow semantics keep the window attack-relevant only for local writers, who already control the store.
5. INFO — packages/store-git/src/index.ts:389 propagates non-ENOENT lstat errors out of initialize; fail-closed direction and practically unreachable after the hooks-dir mkdir succeeded.
6. INFO — an abandoned hint iterator (never returned, aborted, or GC-triggering an event) would retain its consumer until process exit; caller-contract violation, and both in-scope hint implementations plus Board.watch always terminate; release duties documented in packages/core/src/store.ts.
7. COSMETIC — packages/core/test/board.test.ts has a doubled blank line between the last new test and the helper class; no effect.

## Sealed bundle

`/Volumes/Delorean/code/security-scans/sidekick-tmp/20260905T-404-regate/`
(change.diff, head/status at pin, 03_snapshot with SHA256SUMS, this report).
